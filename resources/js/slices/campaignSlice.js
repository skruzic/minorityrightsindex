import {
    createSlice,
    createAsyncThunk,
    createEntityAdapter,
} from '@reduxjs/toolkit';

export const campaignSlice = createSlice({
    name: 'campaign',
});

export default campaignSlice.reducer;
